# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

vars:
  CLI_PROJECT_ROOT: "{{.TASKFILE_DIR}}/cmd/cli"
  APP_PROJECT_ROOT: "{{.TASKFILE_DIR}}/cmd/app"
  BUILD_DIR: "{{.TASKFILE_DIR}}/build"

tasks:
  cli:
    desc: Run codefolio CLI
    dir: "{{.CLI_PROJECT_ROOT}}"
    cmds:
      - go run .
    silent: true
    interactive: true

  app:
    desc: Run Codefolio App
    dir: "{{.APP_PROJECT_ROOT}}"
    cmds:
      - wails dev
      - rm -rf build/bin
    silent: true
    interactive: true

  clean:
    desc: Clean build artifacts
    cmds:
      - rm -rf {{.BUILD_DIR}}
    silent: true

  build:
    desc: Build CLI and App for release
    cmds:
      - task: clean
      - mkdir -p {{.BUILD_DIR}}
      - echo "*" > {{.BUILD_DIR}}/.gitignore
      - task: build-app
      - task: build-cli
    silent: true

  build-cli:
    internal: true
    dir: "{{.CLI_PROJECT_ROOT}}"
    vars:
      EXT:
        sh: if [ "{{OS}}" = "windows" ]; then echo -n ".exe"; fi
    cmds:
      - go build -o {{.BUILD_DIR}}/codefolio{{.EXT}} .
    silent: true

  build-app:
    internal: true
    dir: "{{.APP_PROJECT_ROOT}}"
    cmds:
      - wails build > /dev/null
      - mv build/bin/* {{.BUILD_DIR}}/
      - rm -rf build/bin
    silent: true
